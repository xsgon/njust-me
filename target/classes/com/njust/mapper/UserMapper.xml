<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.njust.mapper.UserMapper">


    <select id="getUserById" resultType="com.njust.vo.UserVo">
		SELECT
		id, name, password, email, portrait, role, phone, active,
		create_time as creatTime, update_time as updateTime
		FROM
		USERS where id=#{id}
	</select>

    <update id="updateUser">
          UPDATE USERS set
            name = #{username},
            email = #{email},
            portrait = #{portrait},
            role = #{role},
            phone = #{phone},
            active = #{active},
            update_time = now()
          where id = #{id}
    </update>

    <insert id="insertUser">
      insert into USERS (id,username,password,email,portrait,role,phone,active,create_time,update_time)
      values (#{id},#{username},#{password},#{email},#{portrait},#{role},#{phone},#{active},now(),now())
    </insert>


</mapper>
